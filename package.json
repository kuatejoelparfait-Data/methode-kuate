{
  "name": "methode-kuate-root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w packages/core && npm run build -w packages/agents-dev -w packages/agents-business -w packages/agents-content -w packages/agents-education && npm run build -w packages/cli",
    "postinstall": "npm run build && npm link --workspace=packages/cli",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint packages/*/src/**/*.ts",
    "clean": "rimraf packages/*/dist"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0",
    "tsup": "^8.0.0",
    "vitest": "^2.0.0",
    "eslint": "^9.0.0",
    "rimraf": "^5.0.0"
  },
  "engines": {
    "node": ">=20.12.0"
  }
}
